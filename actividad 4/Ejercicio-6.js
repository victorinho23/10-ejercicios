// Solicita un número entero al usuario
let numero = parseInt(promt("Ingrese un número entero:"));

// Verifica si el número es par o impar
if (numero % 2 === 0){
    alert("El numero es par.");
} else{
    alert("El numero es impar.");
}