// Solicita la velocidad al usuario
let vellocidad = parseFloat(prompt("Ingrese la velocidad del automóvil en km/h:"));

// Verfica si la velocidad es mayo a 80 km/h
if (velocidad > 80){
    alert("¡Va a exceso de velocidad!");
} else{
    alert("La velocidad es adecuada.");
}